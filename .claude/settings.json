{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f || exit 0; case \"$f\" in *.md) npx markdownlint-cli2 --fix \"$f\" 2>/dev/null || true ;; *.mdx) npx prettier --write \"$f\" 2>/dev/null || true ;; esac; }",
            "timeout": 30,
            "statusMessage": "Linting markdown..."
          }
        ]
      }
    ]
  }
}
