---
name: lint-folder
description: Lint a specific docs folder. Faster than full lint when working in one section. Usage: /lint-folder <path>
disable-model-invocation: false
---

Run `npm run lint:folder $ARGUMENTS` and report the output.

If $ARGUMENTS is empty, ask the user which folder to lint before running.
